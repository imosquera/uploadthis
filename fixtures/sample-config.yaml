auth:
    accesskey: myaccesskey
    secretkey: mysupersecretkey
monitordirs: 
    - path: /tmp/fixtures/monitordir/
      bucket: 34
      prehooks:
        - compress
      posthooks:
        - archive 
logdir: /var/log/

