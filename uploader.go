package uploadthis

import (
	"log"
)

func UploadFile(file string) {
	println(file)
	// s3 := GetS3()
	// bucket := s3.Bucket("pointabout")
	// _, err := bucket.Get("Certificates.p12")
	// if err != nil {
	// 	log.Fatal(err)
	// }
}
